{% extends 'base.html.twig' %}

{% block body %}
    <h1>Добавить СМИ</h1>
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" class="btn btn-secondary" value="{{'forms.buttons.create'|trans }}" />
    {{ form_end(form) }}
    <br>
    <p><a href="{{ path('admin_smi_index') }}">{{'messages.tolist'|trans }}</a></p>

{% endblock %}
