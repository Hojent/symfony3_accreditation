{% extends 'base.html.twig' %}

{% block body %}
    <h1>{{'headers.admin.eventtip' |trans }}</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" class="btn btn-secondary" value="{{'forms.buttons.save' |trans }}" />
    {{ form_end(form) }}
    <p><a href="{{ path('admin_evtip_index') }}">{{'messages.tolist'|trans }}</a></p>

{% endblock %}
