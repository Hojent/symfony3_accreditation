{% extends 'base.html.twig' %}

{% block body %}
    <h1>Evtip</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ evtip.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ evtip.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_evtip_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('admin_evtip_edit', { 'id': evtip.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
