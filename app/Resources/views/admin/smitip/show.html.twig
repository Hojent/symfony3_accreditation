{% extends 'base.html.twig' %}

{% block body %}
    <h1>Smitip</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ smitip.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ smitip.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_smitip_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('admin_smitip_edit', { 'id': smitip.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
