{% extends 'base.html.twig' %}

{% block body %}
    <h1>Добавить регион</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <input type="submit" class="btn btn-secondary" value="Создать" />
    {{ form_end(form) }}
    <p><a href="{{ path('region_index') }}">К списку</a></p>
{% endblock %}
